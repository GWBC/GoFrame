{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "cwd": "${workspaceFolder}/bin/",
      "output": "${workspaceFolder}/bin/debug",
      "program": "${workspaceFolder}/src/"
    }
  ]
}
